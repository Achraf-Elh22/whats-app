doctype html
html(lang="en")
        head
            block head
                meta(charset="UTF-8")
                meta(name="viewport" content="width=device-width, initial-scale=1.0")
                link(rel="stylesheet" href="/styles/style.css")
                title Whatsapp | #{title}
                link(rel="stylesheet")
        body
            - var alertMsg = success == "" ? error : success ; 
            if alertMsg != ""
                div(class=`alert alert--${success == "" ? "error" : "success" }`  )= alertMsg

            block content

            block scripts
                script(src="/scripts/bundle.js")

