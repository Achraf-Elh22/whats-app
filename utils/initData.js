const User = requrie("../models/userModel.js");

class initData {
  constructor() {}

  Users() {
    return 1;
  }
  // Users + lastMsg, groups+lastmsg,
}

// TODO:
// - [x] Learn more about the relationship's in MongoDB
// - [x] whats the indexes use for
// - [x] Build the hole DB schema
// - [ ] Build the validations for schema's
// - [ ] Start building INITDATA class (think of what's exactly info we needed in the init )
// - [ ] Build automation script for import and init the Data(json ) auto and integrated in package.json
